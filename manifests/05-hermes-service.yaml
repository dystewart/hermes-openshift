apiVersion: v1
kind: Service
metadata:
  name: hermes-agent
  namespace: hermes
  labels:
    app: hermes-agent
    component: agent
spec:
  type: ClusterIP
  selector:
    app: hermes-agent
    component: agent
  ports:
  - name: http
    port: 8080
    targetPort: 8080
    protocol: TCP
